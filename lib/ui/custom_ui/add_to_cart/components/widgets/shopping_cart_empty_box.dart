import 'package:flutter/material.dart';

import '../../../../vendor_ui/add_to_cart/components/widgets/shopping_card_empty_box.dart';

class CustomShoppingCartEmptyBox extends StatelessWidget {
  const CustomShoppingCartEmptyBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ShoppingCartEmptyBox();
  }
}