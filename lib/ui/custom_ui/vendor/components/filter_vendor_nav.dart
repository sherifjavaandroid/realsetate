import 'package:flutter/material.dart';
import '../../../vendor_ui/vendor/components/filter_vendor_nav.dart';

class CustomFilterVendorNav extends StatelessWidget {
  const CustomFilterVendorNav({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const FilterVendorNav();
  }
}
