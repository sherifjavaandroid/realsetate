import 'package:flutter/material.dart';

import '../../../../../vendor_ui/chat/component/list/widgets/chat_seller_list_view.dart';

class CustomChatSellerListView extends StatelessWidget {
  const CustomChatSellerListView({Key? key})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ChatSellerListView();
  }
}
