import 'package:flutter/material.dart';

import '../../../../../../vendor_ui/item/list_with_filter/components/item/widgets/filter_icon.dart';

class CustomFilterIconWidget extends StatelessWidget {
  const CustomFilterIconWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const FilterIconWidget();
  }
}
