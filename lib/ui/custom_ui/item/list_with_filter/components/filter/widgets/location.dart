import 'package:flutter/material.dart';

import '../../../../../../vendor_ui/item/list_with_filter/components/filter/widgets/location.dart';

class CustomLocationDropDownButton extends StatelessWidget {
  const CustomLocationDropDownButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const LocationDropDownButton();
  }
}
