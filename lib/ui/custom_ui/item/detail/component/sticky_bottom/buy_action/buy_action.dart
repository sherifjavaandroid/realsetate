import 'package:flutter/material.dart';
import '../../../../../../vendor_ui/item/detail/component/sticky_bottom/buy_action/buy_action.dart';

class CustomBuyAction extends StatelessWidget {
  const CustomBuyAction({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const BuyAction();
  }
}
