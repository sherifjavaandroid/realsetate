import 'package:flutter/material.dart';

import '../../../vendor_ui/order_sort_by/view/order_sort_view.dart';

class CustomOrderSortView extends StatelessWidget {
  const CustomOrderSortView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const OrderSortView();
  }
}