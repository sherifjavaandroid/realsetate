import 'package:flutter/material.dart';

import '../../../vendor_ui/app_info/component/header_widget.dart';

class CustomHeaderImageWidget extends StatelessWidget {
  const CustomHeaderImageWidget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const HeaderImageWidget();
  }
}
